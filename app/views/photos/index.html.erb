<h1>Fotos</h1>

<%= content_for :sidebar_options do %>
    <p>Hola desde fotos/index</p>
<%end%>

<%= link_to "Nuevo", new_photo_path %>
<% @photos.each do |photo| %>
    <div>
        <h2><%= photo.title %></h2>
        <%= image_tag photo.image_url %>
        <%= link_to "Ver más", photo %>
    </div>
<% end %>